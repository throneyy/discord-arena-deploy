// Discord Arena — SourceMod ConVar config
// Copy to: cfg/sourcemod/discord_arena.cfg

// Your Discord Arena Node.js API base URL (no trailing slash)
// Example: https://your-railway-app.up.railway.app
da_api_url ""

// Shared HMAC secret (must match GAME_SERVER_HMAC_SECRET in .env)
da_hmac_secret ""

// Match ID — set this via RCON before each match starts
// Example: rcon da_match_id "clxxxxxxxxxxxxxxxxxx"
da_match_id ""
